# Railway Deployment - Troubleshooting Guide

## ✅ Исправлено в последнем обновлении

### 1. ❌ Ошибка подключения к PostgreSQL (ECONNREFUSED)

**Проблема:**
```
Error: connect ECONNREFUSED ::1:5432
Error: connect ECONNREFUSED 127.0.0.1:5432
```

**Причина:**
Backend пытался подключиться к `localhost:5432` вместо использования `DATABASE_URL` от Railway.

**Решение:**
✅ Обновлен `src/db/connection.js` для автоматического использования `DATABASE_URL` если он доступен.

**Что изменилось:**
```javascript
// Теперь backend автоматически определяет Railway DATABASE_URL
const connectionConfig = config.database.url
  ? { 
      connectionString: config.database.url, 
      ssl: { rejectUnauthorized: false } // Railway требует SSL
    }
  : {
      host: config.database.host,
      port: config.database.port,
      database: config.database.name,
      user: config.database.user,
      password: config.database.password,
    };
```

---

### 2. ⚡ Мгновенное обнаружение новых токенов

**Проблема:**
Старая система проверяла новые токены каждые 10 секунд через polling API.

**Решение:**
✅ Добавлен **WebSocket Wallet Tracker** через Helius для МГНОВЕННОГО обнаружения.

**Как работает:**
1. Backend загружает все кошельки разработчиков с миграциями
2. Подписывается на WebSocket Helius для отслеживания транзакций этих кошельков
3. При любой транзакции от отслеживаемого кошелька → мгновенная проверка
4. Если создан новый токен → алерт создается МГНОВЕННО (< 1 секунды)

**Преимущества:**
- ⚡ Мгновенное обнаружение (вместо 10 секунд)
- 🎯 Точное отслеживание конкретных кошельков
- 💰 Меньше API запросов (WebSocket вместо polling)
- 🔔 Реальный real-time мониторинг

---

## 🚀 Повторное развертывание на Railway

### Шаг 1: Railway автоматически обновится

Railway автоматически обнаружит новый commit в GitHub и начнет повторный деплой.

**Проверить:**
1. Зайти в Railway dashboard
2. Открыть ваш проект
3. Перейти в "Deployments"
4. Должен быть новый deployment с коммитом "Fix: Railway DATABASE_URL support..."

---

### Шаг 2: Проверить переменные окружения

В Railway dashboard → Service → Variables убедитесь что есть:

**Автоматически созданные Railway:**
- ✅ `DATABASE_URL` (создается автоматически при добавлении PostgreSQL)

**Нужно добавить вручную:**
- ✅ `HELIUS_RPC_URL` = `https://mainnet.helius-rpc.com/?api-key=14649a76-7c70-443c-b6da-41cffe2543fd`

**Опционально (есть defaults):**
- `NODE_ENV` = `production`
- `PORT` = `3001`
- `HISTORICAL_SCAN_ENABLED` = `true`
- `REALTIME_MONITOR_ENABLED` = `true`

---

### Шаг 3: Дождаться успешного деплоя

**Проверить логи:**
1. Railway dashboard → Deployments → Latest deployment
2. Смотреть логи в реальном времени

**Ожидаемый вывод:**
```
═══════════════════════════════════════════════════════════
🚀 PADRE PUMP.FUN BACKEND SERVICE
═══════════════════════════════════════════════════════════

⚙️  Configuration:
  Environment: production
  Port: 3001
  Database: padre_pump
  Historical Scan: Enabled
  Real-time Monitor: Enabled

═══════════════════════════════════════════════════════════

📊 Testing database connection...
✅ Database connected successfully

🌐 Starting API server...
✅ API server started on port 3001

📚 Historical scan enabled, starting...
...
✅ Historical scan completed!

👁️  Starting real-time monitors...
⚡ Starting instant wallet tracker...
🔌 Connecting to Helius WebSocket...
✅ Connected to Helius WebSocket
📋 Tracking 897 developer wallets with migrations
✅ Subscription confirmed

═══════════════════════════════════════════════════════════
✅ ALL SYSTEMS OPERATIONAL
═══════════════════════════════════════════════════════════

🎯 Backend service is now running 24/7
📡 API available at: http://localhost:3001
👁️  Monitoring for new coins from tracked developers...
```

---

### Шаг 4: Проверить работу API

```bash
# Замените YOUR_URL на ваш Railway URL
export API_URL="https://your-app.railway.app"

# Проверить здоровье
curl $API_URL/api/health

# Должен вернуть:
{
  "success": true,
  "status": "healthy",
  "database": "connected",
  "stats": {
    "totalDevelopers": 897,
    "totalCoins": 12543,
    "totalMigrations": 1234
  }
}

# Проверить разработчиков
curl $API_URL/api/developers?limit=10

# Проверить алерты
curl $API_URL/api/alerts?limit=10
```

---

## 🐛 Если все еще есть проблемы

### Проблема: Database connection failed

**Проверить:**
1. PostgreSQL сервис запущен в Railway
2. `DATABASE_URL` переменная существует
3. Подождать 2-3 минуты после создания PostgreSQL

**Решение:**
```bash
# В Railway CLI
railway variables

# Должна быть переменная DATABASE_URL
# Если нет - пересоздать PostgreSQL сервис
```

---

### Проблема: WebSocket не подключается

**Проверить:**
1. `HELIUS_RPC_URL` правильно указан
2. API ключ Helius валидный
3. Проверить логи на ошибки WebSocket

**Решение:**
Если WebSocket не работает, backend все равно будет работать через polling (каждые 10 секунд).

---

### Проблема: Миграция базы данных не запустилась

**Проверить логи:**
```
✅ Database migration completed successfully!
```

**Если нет:**
```bash
# Установить Railway CLI
npm install -g @railway/cli

# Войти
railway login

# Подключиться к проекту
railway link

# Запустить миграцию вручную
railway run npm run migrate
```

---

### Проблема: Historical scan слишком долгий

**Временно отключить:**
В Railway Variables добавить:
```
HISTORICAL_SCAN_ENABLED=false
```

Backend запустится без исторического скана, но будет работать real-time мониторинг.

Потом можно включить обратно и перезапустить.

---

## 📊 Мониторинг после деплоя

### Проверить метрики Railway

1. Railway dashboard → Service → Metrics
2. Смотреть:
   - CPU usage (должно быть 5-20%)
   - Memory usage (должно быть 200-500MB)
   - Network (должны быть запросы)

### Проверить логи

```bash
# В Railway dashboard
Deployments → Latest → View Logs

# Или через CLI
railway logs
```

### Проверить WebSocket подключение

В логах должно быть:
```
✅ Connected to Helius WebSocket
📋 Tracking 897 developer wallets with migrations
✅ Subscription confirmed
```

---

## ✅ Чеклист успешного деплоя

- [ ] PostgreSQL сервис создан и запущен
- [ ] `DATABASE_URL` автоматически создан Railway
- [ ] `HELIUS_RPC_URL` добавлен в Variables
- [ ] Deployment успешно завершен (зеленый статус)
- [ ] В логах: "✅ ALL SYSTEMS OPERATIONAL"
- [ ] API health check возвращает 200 OK
- [ ] WebSocket подключен к Helius
- [ ] Отслеживаются кошельки разработчиков
- [ ] Можно получить список разработчиков через API

---

## 🎯 Что дальше

После успешного деплоя:

1. **Скопировать Railway URL**
   - Railway dashboard → Service → Settings → Generate Domain
   - Например: `https://padre-pump-backend-production.up.railway.app`

2. **Обновить расширение**
   - Открыть `extension/src/api-client.js`
   - Изменить `API_BASE_URL` на ваш Railway URL
   - Перезагрузить расширение в Chrome

3. **Проверить работу**
   - Открыть расширение
   - Должно быть "Backend Connected"
   - DATABASE tab показывает разработчиков
   - HISTORY tab показывает алерты

4. **Наслаждаться мгновенными алертами!** ⚡

---

## 💡 Советы

1. **Мониторинг логов:**
   ```bash
   railway logs --follow
   ```

2. **Проверка базы данных:**
   ```bash
   railway connect postgres
   \dt  # Показать таблицы
   SELECT COUNT(*) FROM developers;
   SELECT COUNT(*) FROM coins;
   ```

3. **Перезапуск сервиса:**
   Railway dashboard → Service → Settings → Restart

4. **Откат к предыдущей версии:**
   Railway dashboard → Deployments → Previous deployment → Redeploy

---

**Теперь все должно работать!** 🚀
